# 毕方 V0.3：从“会写”升级为“看作品后会规划”

## 本版新增的两条能力

### 1. 拆片仿写

输入对标视频链接、本地视频、逐字稿或截图，先拿到可验证的台词和画面证据，再用五段结构拆出：开头怎么抓人、讲了哪个问题、用了什么场景、中间怎么解释、结尾怎么收。随后按客户行业替换人群、顾虑、证据与承接，输出三条可拍的非洗稿脚本。

参考的 `viral-video-analyzer` 提供了视频下载、抽帧、转写和视觉标注的工程链路；毕方不复制它的“复刻提示词”定位，而把其结构化素材转成商业短视频所需的用户判断题、真实证据和成交承接。参考项目使用 FFmpeg、Whisper、视觉模型和 SSE 组织分析流程。[项目 README](https://github.com/pelpeljakob-creator/viral-video-analyzer)

### 2. 老账号规划

已有账号不再从“你是谁”开始。优先分析三组历史内容：播放最高、私信/成交最多、长期无效但常发；把作品归为流量、信任、成交、无效消耗四类资产，再给未来 30 天方向、内容配比、一周发布表以及 24-48 小时复盘规则。

## 产品结构

```text
已有视频/链接 ─┐
                ├─ 拆片（素材证据 + 五段结构） ─┐
历史作品/数据 ──┘                               ├─ 老账号规划 ─→ 发布 ─→ 数据复盘
                                                └─ 结构仿写 ─→ 审核 ────┘
新行业/新账号 ───────────────────────→ 原有快速诊断/选题/脚本链路
```

## 统一作品证据包

`bifang-evidence` 是 V0.3 的唯一资料层：一条视频的链接、标题、发布时间、逐字稿、关键帧、播放互动、咨询与成交只录入一次。拆片仿写读取它的事实字段；老账号规划读取多条视频的表现与转化字段；发布后复盘只更新同一个文件的 24–48 小时数据。

本机视频分析器的任务目录或 `result.json` 可由 `import-viral-analyzer.mjs` 自动导入。证据包随后直接交给毕方的拆片仿写、老账号规划和复盘能力读取；用户不需要配置或理解任何外部模型运行器。

发布 24–48 小时后，`record-post-publish.mjs` 将播放、完播、评论意图、有效私信和成交回写原证据包；`analyze-account-assets.mjs` 重新判断账号缺口，`build-week-plan.mjs` 输出下一轮 7 天内容角色、开头、证据、承接与观察指标。这样下一周排期不是凭感觉重开，而是由上一周的真实记录驱动。

## V0.3 的关键取舍

- 不把“拉片”做成独立炫技功能；每次拆片必须回答它对账号的流量、信任或成交缺口有什么作用。
- 不把“仿写”做成同义词替换；保留的是内容功能，必须更换行业变量和真实证据。
- 不把“账号规划”变成长问卷；老账号作品和数据优先，个人资料为补充。
- 不虚构算法结论和最佳发布时间；发布建议围绕可执行频率、对照测试和真实数据迭代。

## 开源目录新增

- `bifang-clip/`：拆片、结构迁移和三路仿写。
- `bifang-account-plan/`：老账号作品诊断、内容配比、发布排期和复盘规则。
- `bifang-evidence/`：统一证据包标准、视频分析器适配、一键工作台和老账号资产分组脚本。

## 推荐安装

在原有核心模块外增加：

```text
bifang-clip
bifang-account-plan
```

对于能运行视频分析服务的使用者，可将外部视频分析器输出的逐字稿、关键帧和分段标注直接喂给 `bifang-clip`；毕方本身保持 Skill 交付，避免强制每个安装者部署下载器、FFmpeg、Whisper 与视觉 API。
